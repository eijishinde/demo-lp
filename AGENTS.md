# AGENTS.md｜このリポジトリでの作業ルール

Codexは、このファイルのルールを**すべての指示より優先**して守ること。

---

## 0. このサイトについて

企業の採用サイト(1ページ構成)。文言・画像は差し替え前提のダミー。
HTML / CSS / JavaScript のみで構成され、ビルド工程は存在しない。

```
index.html          全セクション
css/style.css       デザイントークンは :root に集約
js/script.js        ハンバーガーメニューのみ
assets/icons/       Lucide Icons(ISC)/ 再配色済み
assets/img/         写真プレースホルダー(差し替え前提)
```

---

## 1. 実装方針

- HTML、CSS、JavaScript のみを使用する
- React、Vue、Vite、Node.js、npm、jQuery、外部CDN、外部ライブラリは**一切使用しない**
- Webフォントを読み込まない(システムフォント方針。詳細は §4)
- エントリーファイルは `index.html`、CSSは `css/style.css`、JSは `js/script.js` のみ
- 新しいCSS/JSファイルを増やさない
- モバイルファースト。ブレイクポイントは **768px / 1024px** のみ使用する
- 色・フォント・余白は `:root` のCSS変数を使う。**マジックナンバーの直書き禁止**

---

## 2. セクション構造

ページは以下の6セクションで構成されている。順序は上から:

| クラス | id | 内容 |
|---|---|---|
| `.hero` | — | ファーストビュー。斜めカット画像+見出し+SCROLL |
| `.msg` | — | メッセージ。左に本文、右に大見出し |
| `.business` | `#business` | 事業紹介。グレー地+アイコン3列 |
| `.people` | `#people` | 人紹介。左が矢印状にカットされた画像 |
| `.env` | `#env` | 働く環境。緑の矢印ブロック+2×2グリッド |
| `.entry` | `#entry` | エントリー導線。緑の横長バンド |

### セクション操作のルール

- 追加・削除・並べ替えは**必ずセクション単位**で行う
- 新規セクションを追加する場合:
  - 既存の共通クラス(`.label` `.sec-title` `.sec-text` `.more`)を**必ず流用**する
  - 新しいクラスは `.新セクション名__要素` のBEM形式で命名する
  - 上下余白は既存セクションと同じ値(SP 72px / PC 120px)に揃える
  - 挿入位置が指示にない場合は、実装せず確認する
- セクションを削除する場合、他セクションのHTML・CSSに一切触れない
- ヘッダーナビ(`.hdr__list`)とフッターナビ(`.ftr__nav`)のリンクも整合させる

---

## 3. デザイントークン(`:root`)

```
--green       #0B5D4C   メインカラー。見出し・ボタン・帯
--green-dark  #094135   ホバー時
--green-light #EAF2F0   淡い背景
--ink         #1E1E1E   本文
--ink-sub     #5A5F5E   補助テキスト
--gray        #F4F5F5   セクション背景
--line        #E2E5E4   罫線
--hdr-h       72px      ヘッダー高さ(scroll-marginと連動)
```

- 配色変更の指示があった場合、**`:root` の値だけを書き換える**。個別セレクタに色を直書きしない
- `--hdr-h` を変更した場合、`.hdr__nav` の `inset` も追従させる

---

## 4. タイポグラフィ(重要)

Webフォントは使わない。システムフォント優先のスタックを `--font` / `--font-en` に定義済み。

- **和文見出し**(`.sec-title` `.hero__title` `.msg__title` `.entry__title`)
  - `font-weight: 400`、`letter-spacing: .14〜.2em`、`font-feature-settings:"palt" 0`(ベタ組み)
  - 太字にしない。ゴシックの線を細く保つのがこのデザインの前提
- **本文**:`font-feature-settings:"palt" 1`(詰め組み)、`line-height: 2.1〜2.4`
- **欧文ラベル**(BUSINESS / PEOPLE / SCROLL / SAMPLE INC.):`--font-en` を指定し、`letter-spacing: .22em` 前後
- 字間を広げる変更を行った場合、**見出しの折り返し行数が増えていないか必ず確認する**

---

## 5. 素材のルール

- アイコンは `assets/icons/` 内のSVGのみ使用する
  - 使用可能: arrow-right / arrow-up-right / book-open / building-2 / calendar / globe / heart / menu / trending-up / users / x
  - すべて `stroke="#0B5D4C"` `stroke-width="1.3"` に再配色済み。**新規追加時も同じ規則に揃える**
- 画像は `assets/img/` 内のファイルのみ使用する
  - `hero.svg` `people.svg` は写真差し替え前提のプレースホルダー
  - 差し替え時は同じファイル名(拡張子違い可)で置き、`src` のみ変更する
- 一覧にない素材が必要な場合は、**実装せず「要確認」として報告する**
- 禁止:
  - 絵文字をアイコン代わりに使う
  - CSSやdivで図形アイコンを自作する
  - 外部URLの画像・プレースホルダー画像サービス(placehold.co 等)を参照する
  - 画像をBase64で埋め込む

---

## 6. コンテンツのルール

- 掲載文章を指示なく変更しない
- 存在しない実績・数値・受賞歴・顧客名・社員名を追加しない
- 「ダミーテキストがここに入ります。」等のダミー文言は、**指示がない限りそのまま維持する**
- 電話番号・住所・メールアドレスは指示された値のみ使用する
- 指示されていない箇所を変更しない

---

## 7. 修正指示の受け方

- 修正指示は「維持するもの / 修正するもの / 修正しないもの」の3区分で受け取る
- **1回の指示で修正するのは最大3箇所**。それを超える場合は優先順位を確認する
- 「維持するもの」「修正しないもの」に触れる必要が生じた場合は、実装せず報告する

---

## 8. 作業手順

1. `index.html` と `css/style.css` の該当箇所を確認する
2. 実装前に、**変更対象ファイルと変更内容の計画を提示する**
3. 実装後、変更内容を箇条書きで説明する
4. 以下を自己確認し、結果を報告する
   - PC(1440px)とスマートフォン(375px)で表示崩れがないか
   - 横スクロールが発生していないか
   - 見出しの折り返しが不自然になっていないか
5. **変更は必ず Pull Request として提出する**(mainへ直接コミットしない)

---

## 9. デザイン品質の禁止パターン

このサイトは「余白の広い、線の細い、静かなコーポレートデザイン」である。以下は方針違反:

- グラデーションの多用(現在はヒーロー画像とプレースホルダーのみ)
- 影(box-shadow)の追加 — このデザインは影を使わない
- 角丸の追加 — 円形要素(`.more__circle` `.entry__btn` `.ftr__sns`)以外は直角
- 3D表現・パララックス・複雑なアニメーション
- 1画面に3色以上のアクセントカラー
- 見出しの太字化(`font-weight: 700` 以上)
- 行間・余白の圧縮(このデザインは余白が主役)
- 絵文字の装飾利用
