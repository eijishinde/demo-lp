# AGENTS.md｜このリポジトリでの作業ルール

Codexは、このファイルのルールを**すべての指示より優先**して守ること。

## 実装方針

- HTML、CSS、JavaScriptのみを使用する
- React、Vue、Vite、Node.js、npm、外部CDN、外部ライブラリは使用しない
- エントリーファイルは `index.html`、CSSは `css/style.css`、JSは `js/script.js` のみ
- PC(1024px〜)とスマートフォン(375px〜)の両方に対応する(モバイルファースト)
- デザインの数値・色・フォントは `docs/design-spec.md` を正とする
- 色・フォント・余白は `:root` のCSS変数を使う。マジックナンバーの直書き禁止

## セクション構造(重要)

- ページは `<section data-module="...">` 単位で独立している
- セクションの追加・削除・並べ替えは、**必ずセクション単位**で行う
- セクションを新規追加する場合:
  - 既存セクションの見出しスタイル(`.section__title`)・余白(`.section`)・配色を踏襲する
  - クラス名は `.新セクション名__要素` のBEM形式で新設し、既存クラスを流用してよい
  - 挿入位置は指示で指定された場所のみ。指定がなければ実装せず確認する
- セクションを削除する場合、他のセクションのHTML・CSSに一切触れない

## 素材のルール(重要)

- アイコンは `assets/icons/` 内のSVGのみ使用する。一覧は `docs/asset-list.md`
- イラストは `assets/illustrations/` 内のSVGのみ使用する
- 写真は `assets/photos/` 内のファイルのみ使用する
- 一覧にない素材が必要な場合は、実装せず「要確認」として報告する
- 禁止: 絵文字をアイコン代わりに使う/CSSやdivで図形アイコンを自作する/外部URLの画像・プレースホルダー画像サービスを参照する
- イラストSVGの色変更は、`docs/design-spec.md` のアクセントカラーに合わせる場合のみ可

## コンテンツのルール

- 掲載文章を指示なく変更しない
- 存在しない実績・数値・受賞歴・顧客名を追加しない
- 指示されていない箇所を変更しない
- 電話番号・住所・メールアドレスは指示された値のみ使用する

## 修正指示の受け方

- 修正指示は「維持するもの/修正するもの/修正しないもの」の3区分で受け取る
- **1回の指示で修正するのは最大3箇所**。それを超える指示は、優先順位を確認する
- 「維持するもの」「修正しないもの」に触れる必要が生じた場合は、実装せず報告する

## 作業手順

1. `docs/lp-brief.md`・`docs/design-spec.md`・`docs/asset-list.md` を確認する
2. 実装前に、変更対象ファイルと変更内容の計画を提示する
3. 実装後、変更内容を箇条書きで説明する
4. PC(1024px)とスマートフォン(375px)の表示崩れがないか自己確認し、結果を報告する

## デザイン品質の禁止パターン

- 過剰なグラデーション・虹色・ネオン発光の多用
- 3D表現・複雑なアニメーション・パララックス
- 1セクション内に4色以上のアクセントカラー
- 影の多重掛け(box-shadowはトークン `--shadow` のみ)
- 絵文字の装飾利用
